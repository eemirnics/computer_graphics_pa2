from point_and_vector import *

class ball:

    def __init__(self):
        self.pos = point(400, 400)
        self.radius = 7
        self.motion = Vector(350, -400)
        self.color = [1.0, 1.0, 1.0]

